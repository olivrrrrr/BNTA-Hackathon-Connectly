class User {
    constructor(id, name, jobTitle, email, tags, events) {
        this.id = id,
        this.name = name,
        this.jobTitle = jobTitle,
        this.email = email,
        this.tags = tags,
        this.events = events
    }
}

export default User;